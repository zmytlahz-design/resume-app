# Deprecated: interview demo runs frontend via Vite dev server only.
# Keep this file as a placeholder to avoid broken references in older docs/scripts.
# Runtime proxy is handled by `frontend/vite.config.js` (`/api` -> `http://localhost:8011`).
